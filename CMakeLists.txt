cmake_minimum_required(VERSION 3.21)
option(DATAPUMP_BUILD_GUI "Build the native C++ desktop application" ON)
set(DATAPUMP_GUI_BACKEND "fltk" CACHE STRING "Single GUI backend to compile (fltk or rev)")
set_property(CACHE DATAPUMP_GUI_BACKEND PROPERTY STRINGS fltk rev)
option(DATAPUMP_PORTABLE "Install a relocatable bundle with native dependencies" ON)
option(DATAPUMP_SANITIZERS "Enable address and undefined behavior sanitizers" OFF)
option(DATAPUMP_TEST_NATIVE_GUI "Register GUI conformance tests requiring a private display" OFF)
if(DATAPUMP_PORTABLE)
  set(OPENSSL_USE_STATIC_LIBS TRUE CACHE BOOL "Link OpenSSL statically")
  set(OPENSSL_MSVC_STATIC_RT TRUE CACHE BOOL "Use OpenSSL built for the static MSVC runtime")
  set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>" CACHE STRING "MSVC runtime")
endif()
project(DataPump VERSION 0.7.2 LANGUAGES C CXX)
include(cmake/BuildDependencies.cmake)
include(cmake/SdkBuild.cmake)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
if(MSVC)
  add_compile_options(/utf-8)
endif()
find_package(OpenSSL 3.0 REQUIRED COMPONENTS Crypto)
find_package(Threads REQUIRED)
include(cmake/VendoredLzma.cmake)
add_library(datapump src/crypto.cpp src/stream_codec.cpp src/recovery.cpp src/attachment.cpp src/boundary_sync.cpp src/compression_short.cpp src/compression_lzma.cpp src/modem.cpp src/streaming_modem.cpp src/pattern_code.cpp src/pattern_receiver.cpp src/pattern_correlator.cpp src/search_parallel.cpp src/channel.cpp src/runtime.cpp src/audio.cpp src/resampler.cpp src/qr.cpp src/transfer.cpp src/tuning.cpp src/live.cpp src/signal_view.cpp third_party/qrcodegen/qrcodegen.cpp)
# Scalar, cached and batched receiver paths must preserve identical arithmetic.
# AArch64 can fuse multiply/add by default where generic x86-64 cannot. Keep
# separate rounding in both the library and its independent reference tests.
if(CMAKE_CXX_COMPILER_ID MATCHES "^(GNU|Clang|AppleClang)$")
  if(CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    target_compile_options(datapump PUBLIC "$<$<COMPILE_LANGUAGE:CXX>:/clang:-ffp-contract=off>")
  else()
    target_compile_options(datapump PUBLIC "$<$<COMPILE_LANGUAGE:CXX>:-ffp-contract=off>")
  endif()
endif()
target_sources(datapump PRIVATE src/pattern_correlator_batch.cpp src/pattern_fft_batch.cpp src/simulation_estimate.cpp src/lpi_estimate.cpp src/pattern_search.cpp src/correlation_experiment.cpp)
target_sources(datapump PRIVATE src/receiver_probability.cpp src/receiver_probability_differential.cpp)
target_include_directories(datapump PUBLIC include)
target_include_directories(datapump PRIVATE third_party/qrcodegen)
target_include_directories(datapump PRIVATE third_party/xz/src/liblzma/api)
target_link_libraries(datapump PUBLIC OpenSSL::Crypto Threads::Threads ${CMAKE_DL_LIBS})
target_link_libraries(datapump PRIVATE liblzma)
if(DATAPUMP_PORTABLE AND CMAKE_CXX_COMPILER_ID STREQUAL "GNU" AND NOT DATAPUMP_SANITIZERS)
  target_link_options(datapump PUBLIC -static-libstdc++ -static-libgcc)
endif()
if(WIN32)
  target_link_libraries(datapump PRIVATE winmm advapi32)
endif()
if(MSVC)
  target_compile_options(datapump PRIVATE /W4 /permissive-)
else()
  target_compile_options(datapump PRIVATE -Wall -Wextra -Wpedantic -Wconversion -Wshadow)
  if(DATAPUMP_SANITIZERS)
    # Keep instrumented DSP practical in Debug without changing its -g flags
    # or assertions. PUBLIC also covers the CLI, GUI and linked test targets.
    target_compile_options(datapump PUBLIC -O1 -fsanitize=address,undefined -fno-omit-frame-pointer)
    target_link_options(datapump PUBLIC -fsanitize=address,undefined)
  endif()
endif()
if(NOT MSVC)
  # Preserve the pinned upstream QR source; project-only conversion warnings
  # do not imply a requirement to modify vendored, independently tested code.
  set_source_files_properties(third_party/qrcodegen/qrcodegen.cpp PROPERTIES COMPILE_OPTIONS "-Wno-conversion")
endif()
# Independent legacy radio-text DSP. Only the unchanged hardware API is shared.
add_library(datapump_legacy STATIC EXCLUDE_FROM_ALL src/legacy/modem.cpp src/legacy/psk.cpp
  src/legacy/olivia.cpp src/legacy/session.cpp)
target_include_directories(datapump_legacy PUBLIC include)
target_link_libraries(datapump_legacy PRIVATE datapump)
add_custom_target(datapump_legacy_boundary
  COMMAND ${CMAKE_COMMAND} -DROOT=${CMAKE_CURRENT_SOURCE_DIR}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/legacy_boundary.cmake
  COMMENT "Checking Legacy modem isolation" VERBATIM)
add_dependencies(datapump_legacy datapump_legacy_boundary)
if(MSVC)
  target_compile_options(datapump_legacy PRIVATE /W4 /permissive-)
else()
  target_compile_options(datapump_legacy PRIVATE -Wall -Wextra -Wpedantic)
endif()
add_executable(pump src/main.cpp)
target_link_libraries(pump PRIVATE datapump)
add_library(datapump_fast STATIC src/fast/profile.cpp src/fast/preset.cpp src/fast/codec.cpp src/fast/compression.cpp src/fast/attachment.cpp
  src/fast/modem.cpp src/fast/acoustic_ofdm.cpp src/fast/session.cpp src/fast/file_transfer.cpp src/fast/telemetry.cpp src/fast/ldpc.cpp)
target_include_directories(datapump_fast PUBLIC include)
target_include_directories(datapump_fast PRIVATE third_party/xz/src/liblzma/api)
# Only unchanged crypto, GF(256) and hardware utilities cross this boundary.
# Source-level dependency checks below prohibit regular signal processing.
target_link_libraries(datapump_fast PRIVATE datapump liblzma)
add_custom_target(datapump_fast_boundary
  COMMAND ${CMAKE_COMMAND} -DROOT=${CMAKE_CURRENT_SOURCE_DIR}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/fast_boundary.cmake
  COMMENT "Checking regular/fast DSP isolation" VERBATIM)
add_dependencies(datapump_fast datapump_fast_boundary)
target_sources(pump PRIVATE src/fast/cli.cpp)
target_link_libraries(pump PRIVATE datapump_fast)
if(MSVC)
  target_compile_options(datapump_fast PRIVATE /W4 /permissive-)
else()
  target_compile_options(datapump_fast PRIVATE -Wall -Wextra -Wpedantic)
endif()
target_compile_definitions(pump PRIVATE DATAPUMP_VERSION="${PROJECT_VERSION}")
if(MSVC)
  target_link_options(pump PRIVATE "/MANIFEST:EMBED" "/MANIFESTINPUT:${CMAKE_CURRENT_SOURCE_DIR}/src/pump.manifest")
endif()
set(datapump_executables pump)
include(CTest)
if(DATAPUMP_BUILD_GUI)
  include(cmake/NativeGui.cmake)
  list(APPEND datapump_executables datapump-gui)
endif()
add_custom_target(datapump-apps DEPENDS ${datapump_executables})
if(BUILD_TESTING)
  include(cmake/GuiShared.cmake)
  add_executable(test_speculation tests/test_speculation.c)
  target_include_directories(test_speculation PRIVATE include)
  set_property(TARGET test_speculation PROPERTY INTERPROCEDURAL_OPTIMIZATION FALSE)
  if(CMAKE_C_COMPILER_ID MATCHES "^(GNU|Clang|AppleClang)$")
    target_compile_options(test_speculation PRIVATE -O2 -fno-lto)
    target_link_options(test_speculation PRIVATE -fno-lto)
  endif()
  if(DATAPUMP_SANITIZERS AND NOT MSVC)
    target_compile_options(test_speculation PRIVATE -O1 -fsanitize=address,undefined -fno-omit-frame-pointer)
    target_link_options(test_speculation PRIVATE -fsanitize=address,undefined)
  endif()
  add_test(NAME speculation COMMAND test_speculation)
  foreach(component psk olivia session simulation)
    add_executable(test_legacy_${component} tests/test_legacy_${component}.cpp)
    target_link_libraries(test_legacy_${component} PRIVATE datapump_legacy)
    add_test(NAME legacy_${component} COMMAND test_legacy_${component})
  endforeach()
  add_test(NAME legacy_boundary COMMAND ${CMAKE_COMMAND} -DROOT=${CMAKE_CURRENT_SOURCE_DIR}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/legacy_boundary.cmake)
  add_executable(test_gui_legacy tests/test_gui_legacy.cpp)
  target_link_libraries(test_gui_legacy PRIVATE datapump_gui_application datapump_legacy datapump)
  add_test(NAME gui_legacy COMMAND test_gui_legacy)
  set_tests_properties(gui_legacy PROPERTIES LABELS "gui")
  add_executable(test_gui_legacy_live tests/test_gui_legacy_live.cpp)
  target_link_libraries(test_gui_legacy_live PRIVATE datapump_gui_application datapump_legacy)
  add_test(NAME gui_legacy_live COMMAND test_gui_legacy_live)
  set_tests_properties(gui_legacy_live PROPERTIES LABELS "gui")
  # The scripted Session replaces only worker scheduling; use the production
  # controller, plots and codecs without also linking the real Session object.
  add_executable(test_gui_legacy_poll tests/test_gui_legacy_poll.cpp
    src/gui/legacy/controller.cpp src/gui/legacy/plots.cpp
    src/legacy/modem.cpp src/legacy/psk.cpp src/legacy/olivia.cpp)
  target_include_directories(test_gui_legacy_poll PRIVATE src/gui include)
  target_link_libraries(test_gui_legacy_poll PRIVATE Threads::Threads)
  add_dependencies(test_gui_legacy_poll datapump_gui_boundary datapump_legacy_boundary)
  if(DATAPUMP_SANITIZERS AND NOT MSVC)
    target_compile_options(test_gui_legacy_poll PRIVATE -O1 -fsanitize=address,undefined -fno-omit-frame-pointer)
    target_link_options(test_gui_legacy_poll PRIVATE -fsanitize=address,undefined)
  endif()
  add_test(NAME gui_legacy_poll COMMAND test_gui_legacy_poll)
  set_tests_properties(gui_legacy_poll PROPERTIES LABELS "gui")
  foreach(component codec compression attachment modem files session transfer telemetry ldpc qam acoustic acoustic_short acoustic_short_weak ofdm_presence radio presets low_rate pilot_presence output_power)
    add_executable(test_fast_${component} tests/test_fast_${component}.cpp)
    target_link_libraries(test_fast_${component} PRIVATE datapump_fast datapump)
    add_test(NAME fast_${component} COMMAND test_fast_${component})
  endforeach()
  add_test(NAME fast_acoustic_short_convolutional COMMAND test_fast_acoustic_short_weak --legacy-convolutional)
  foreach(snr -6 -3 0 6 13)
    foreach(fixture public_text keyed_attachment)
      add_test(NAME fast_acoustic_short_snr_${snr}_${fixture}
        COMMAND test_fast_acoustic_short --snr=${snr} ${fixture})
    endforeach()
  endforeach()
  add_executable(fast_regression tools/fast_regression.cpp)
  target_link_libraries(fast_regression PRIVATE datapump_fast datapump)
  add_executable(fast_cable_probe EXCLUDE_FROM_ALL tools/fast_cable_probe.cpp)
  target_link_libraries(fast_cable_probe PRIVATE datapump_fast datapump)
  add_executable(acoustic_known_symbols EXCLUDE_FROM_ALL tools/acoustic_known_symbols.cpp)
  target_link_libraries(acoustic_known_symbols PRIVATE datapump_fast datapump)
  add_executable(fast_short_ldpc_compare EXCLUDE_FROM_ALL tools/fast_short_ldpc_compare.cpp)
  target_link_libraries(fast_short_ldpc_compare PRIVATE datapump_fast datapump)
  add_executable(fast_ldpc_benchmark EXCLUDE_FROM_ALL tools/fast_ldpc_benchmark.cpp)
  target_link_libraries(fast_ldpc_benchmark PRIVATE datapump_fast datapump)
  add_test(NAME fast_boundary COMMAND ${CMAKE_COMMAND} -DROOT=${CMAKE_CURRENT_SOURCE_DIR}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/fast_boundary.cmake)
  add_executable(test_gui_fast tests/test_gui_fast.cpp)
  target_link_libraries(test_gui_fast PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_fast COMMAND test_gui_fast)
  set_tests_properties(gui_fast PROPERTIES LABELS "gui")
  add_executable(test_gui_fast_live tests/test_gui_fast_live.cpp)
  target_link_libraries(test_gui_fast_live PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_fast_live COMMAND test_gui_fast_live)
  set_tests_properties(gui_fast_live PROPERTIES LABELS "gui")
  add_test(NAME vendored_lzma_source COMMAND ${CMAKE_COMMAND}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/vendored_lzma.cmake)
  add_executable(benchmark_receiver EXCLUDE_FROM_ALL tools/benchmark_receiver.cpp)
  add_executable(benchmark_receive_processing EXCLUDE_FROM_ALL tools/benchmark_receive_processing.cpp)
  target_link_libraries(benchmark_receive_processing PRIVATE datapump datapump_fast)
  add_executable(benchmark_robust_mitigations EXCLUDE_FROM_ALL tools/benchmark_robust_mitigations.cpp)
  target_link_libraries(benchmark_robust_mitigations PRIVATE datapump)
  target_link_libraries(benchmark_receiver PRIVATE datapump)
  add_executable(benchmark_correlator EXCLUDE_FROM_ALL tools/benchmark_correlator.cpp)
  target_link_libraries(benchmark_correlator PRIVATE datapump)
  add_executable(benchmark_simulation EXCLUDE_FROM_ALL tools/benchmark_simulation.cpp)
  target_link_libraries(benchmark_simulation PRIVATE datapump)
  foreach(component crypto keyring stream_codec recovery attachment stream_receive boundary_sync compression_short compression_lzma modem streaming_modem noise_receive pattern_code pattern_receiver pattern_correlator pattern_drift pattern_differential receiver_differential pattern_fft_batch pattern_correlator_batch pattern_search search_parallel pulse_shaping pattern_transfer regressions runtime qr transfer tuning simulation_estimate receiver_probability differential_probability differential_receiver_probability lpi_estimate correlation_experiment weak_signal live live_resources live_profiles live_receptions live_transmit_lock resampler audio_rates signal_view channel)
    add_executable(test_${component} tests/test_${component}.cpp)
    target_link_libraries(test_${component} PRIVATE datapump)
    add_test(NAME ${component} COMMAND test_${component})
  endforeach()
  target_include_directories(test_differential_probability PRIVATE src)
  # This calibration starts four workers internally; account for them in CTest
  # so small parallel runs do not overlap it with another CPU-heavy suite.
  set_tests_properties(differential_receiver_probability PROPERTIES
    LABELS "calibration" PROCESSORS 4)
  # Shared UI models and policies need no toolkit, including in CLI-only builds.
  add_executable(test_gui_layout tests/test_gui_layout.cpp)
  add_test(NAME gui_layout COMMAND test_gui_layout)
  # These targets deliberately have no modem or toolkit include/link dependency.
  add_executable(test_gui_contract tests/test_gui_contract.cpp)
  target_include_directories(test_gui_contract PRIVATE src/gui)
  add_test(NAME gui_contract COMMAND test_gui_contract)
  add_executable(test_gui_smoke_replay tests/test_gui_smoke_replay.cpp)
  target_include_directories(test_gui_smoke_replay PRIVATE src/gui include)
  add_test(NAME gui_smoke_replay COMMAND test_gui_smoke_replay)
  set_tests_properties(gui_smoke_replay PROPERTIES LABELS gui)
  add_executable(test_rev_event_pump tests/test_rev_event_pump.cpp)
  target_include_directories(test_rev_event_pump PRIVATE third_party/rev/external)
  if(WIN32)
    target_link_libraries(test_rev_event_pump PRIVATE user32)
  else()
    target_include_directories(test_rev_event_pump PRIVATE tests/rev_event_stub)
  endif()
  add_test(NAME rev_event_pump COMMAND test_rev_event_pump)
  set_tests_properties(rev_event_pump PROPERTIES LABELS gui TIMEOUT 10)
  # Compile the same canary with the adapter's actual application dependency.
  # This catches modem include paths leaking back through PUBLIC target usage.
  add_executable(test_gui_link_boundary tests/test_gui_contract.cpp)
  target_link_libraries(test_gui_link_boundary PRIVATE datapump_gui_application)
  add_test(NAME gui_link_boundary COMMAND test_gui_link_boundary)
  add_executable(test_document_layout tests/test_document_layout.cpp)
  target_include_directories(test_document_layout PRIVATE src/gui)
  add_test(NAME gui_document_layout COMMAND test_document_layout)
  add_executable(test_gui_interactions tests/test_gui_interactions.cpp)
  target_include_directories(test_gui_interactions PRIVATE src/gui)
  add_test(NAME gui_interactions COMMAND test_gui_interactions)
  add_executable(test_gui_services tests/test_gui_services.cpp)
  target_include_directories(test_gui_services PRIVATE src/gui)
  add_test(NAME gui_services COMMAND test_gui_services)
  add_executable(test_gui_bindings tests/test_gui_bindings.cpp)
  target_include_directories(test_gui_bindings PRIVATE src/gui)
  add_test(NAME gui_bindings COMMAND test_gui_bindings)
  add_executable(test_record_reconciliation tests/test_record_reconciliation.cpp)
  target_include_directories(test_record_reconciliation PRIVATE src/gui)
  add_test(NAME gui_record_reconciliation COMMAND test_record_reconciliation)
  add_executable(test_document_presentation tests/test_document_presentation.cpp)
  target_include_directories(test_document_presentation PRIVATE src/gui)
  add_test(NAME gui_document_presentation COMMAND test_document_presentation)
  add_executable(test_gui_chrome tests/test_gui_chrome.cpp)
  target_include_directories(test_gui_chrome PRIVATE src/gui)
  add_test(NAME gui_chrome COMMAND test_gui_chrome)
  add_executable(test_inspection tests/test_inspection.cpp)
  target_include_directories(test_inspection PRIVATE src/gui)
  target_link_libraries(test_inspection PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_inspection COMMAND test_inspection)
  add_executable(test_pattern_space tests/test_pattern_space.cpp)
  target_link_libraries(test_pattern_space PRIVATE datapump)
  add_test(NAME gui_pattern_space COMMAND test_pattern_space)
  add_executable(test_gui_plots tests/test_gui_plots.cpp)
  target_link_libraries(test_gui_plots PRIVATE datapump)
  add_test(NAME gui_plots COMMAND test_gui_plots)
  add_executable(test_rev_utf8 tests/test_rev_utf8.cpp)
  add_test(NAME rev_utf8 COMMAND test_rev_utf8)
  add_executable(test_gui_bitmaps tests/test_gui_bitmaps.cpp)
  target_include_directories(test_gui_bitmaps PRIVATE src/gui)
  target_link_libraries(test_gui_bitmaps PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_bitmaps COMMAND test_gui_bitmaps)
  add_executable(test_inspection_page tests/test_inspection_page.cpp)
  target_link_libraries(test_inspection_page PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_inspection_page COMMAND test_inspection_page)
  add_executable(test_binary_editor tests/test_binary_editor.cpp)
  target_include_directories(test_binary_editor PRIVATE src/gui)
  target_link_libraries(test_binary_editor PRIVATE datapump)
  add_test(NAME gui_binary_editor COMMAND test_binary_editor)
  add_executable(test_gui_controller tests/test_gui_controller.cpp)
  target_include_directories(test_gui_controller PRIVATE src/gui)
  target_link_libraries(test_gui_controller PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_controller COMMAND test_gui_controller)
  add_executable(test_profile_reference tests/test_profile_reference.cpp)
  target_link_libraries(test_profile_reference PRIVATE datapump)
  add_test(NAME gui_profile_reference COMMAND test_profile_reference)
  add_executable(test_gui_link_planner tests/test_gui_link_planner.cpp)
  target_link_libraries(test_gui_link_planner PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_link_planner COMMAND test_gui_link_planner)
  add_executable(test_link_planner_curves tests/test_link_planner_curves.cpp)
  target_link_libraries(test_link_planner_curves PRIVATE datapump_gui_application datapump)
  add_test(NAME link_planner_curves COMMAND test_link_planner_curves)
  add_executable(test_gui_launch_command tests/test_gui_launch_command.cpp)
  target_link_libraries(test_gui_launch_command PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_launch_command COMMAND test_gui_launch_command)
  add_executable(test_gui_launch_settings tests/test_gui_launch_settings.cpp)
  target_link_libraries(test_gui_launch_settings PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_launch_settings COMMAND test_gui_launch_settings)
  set_tests_properties(gui_launch_command gui_launch_settings PROPERTIES TIMEOUT 120)
  add_executable(test_gui_application tests/test_gui_application.cpp)
  target_link_libraries(test_gui_application PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_application COMMAND test_gui_application)
  add_executable(test_gui_overlay tests/test_gui_overlay.cpp)
  target_link_libraries(test_gui_overlay PRIVATE datapump_gui_application)
  add_test(NAME gui_overlay COMMAND test_gui_overlay)
  add_test(NAME gui_adapter_boundary COMMAND ${CMAKE_COMMAND}
    -DROOT=${CMAKE_CURRENT_SOURCE_DIR} -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/gui_adapter_boundary.cmake)
  add_test(NAME gui_boundary_regression COMMAND ${CMAKE_COMMAND}
    -DROOT=${CMAKE_CURRENT_SOURCE_DIR} -DBINARY_DIR=${CMAKE_CURRENT_BINARY_DIR}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/gui_boundary_regression.cmake)
  add_executable(test_gui_native tests/test_gui_native.cpp)
  target_include_directories(test_gui_native PRIVATE src/gui)
  target_link_libraries(test_gui_native PRIVATE datapump_gui_application datapump)
  add_test(NAME gui_native_policy COMMAND test_gui_native)
  if(DATAPUMP_BUILD_GUI AND DATAPUMP_GUI_BACKEND STREQUAL "rev")
    add_executable(test_rev_style tests/test_rev_style.cpp)
    set_target_properties(test_rev_style PROPERTIES CXX_STANDARD 23 CXX_SCAN_FOR_MODULES ON)
    target_link_libraries(test_rev_style PRIVATE datapump_rev)
    add_test(NAME rev_style COMMAND test_rev_style)
    set_tests_properties(rev_style PROPERTIES LABELS "gui")
    add_executable(test_rev_adapter tests/test_rev_adapter.cpp src/gui/rev_platform.cpp)
    set_target_properties(test_rev_adapter PROPERTIES CXX_STANDARD 23 CXX_SCAN_FOR_MODULES ON)
    target_link_libraries(test_rev_adapter PRIVATE datapump_gui_application datapump_rev)
    add_executable(test_rev_coordinates tests/test_rev_coordinates.cpp)
    set_target_properties(test_rev_coordinates PROPERTIES CXX_STANDARD 23 CXX_SCAN_FOR_MODULES ON)
    target_link_libraries(test_rev_coordinates PRIVATE datapump_rev ${CMAKE_DL_LIBS})
    add_executable(test_rev_platform tests/test_rev_platform.cpp src/gui/rev_platform.cpp)
    target_include_directories(test_rev_platform PRIVATE src/gui include)
    if(WIN32)
      target_link_libraries(test_rev_platform PRIVATE user32 shell32)
    else()
      target_link_libraries(test_rev_platform PRIVATE X11::X11)
    endif()
  endif()
  if(DATAPUMP_BUILD_GUI)
    add_test(NAME gui_self_check COMMAND datapump-gui --self-check)
    set_tests_properties(gui_self_check PROPERTIES LABELS "gui")
    if(DATAPUMP_TEST_NATIVE_GUI)
      set(GUI_SMOKE_TIMEOUT 300 CACHE STRING "Overall native GUI workflow allowance in seconds (10 to 600)")
      include("${CMAKE_CURRENT_SOURCE_DIR}/tools/gui-smoke-timeout.cmake")
      add_test(NAME gui_workflow COMMAND datapump-gui --smoke-test --smoke-timeout "${GUI_SMOKE_TIMEOUT}")
      set_tests_properties(gui_workflow PROPERTIES LABELS "gui;native_gui"
        TIMEOUT "${GUI_SMOKE_PROCESS_TIMEOUT}" RUN_SERIAL TRUE)
      if(DATAPUMP_GUI_BACKEND STREQUAL "fltk")
        add_test(NAME gui_adapter_conformance COMMAND test_fltk_adapter)
        add_test(NAME gui_document_conformance COMMAND test_fltk_document)
        set_tests_properties(gui_document_conformance PROPERTIES LABELS "gui;native_gui" TIMEOUT 60 RUN_SERIAL TRUE)
      else()
        add_test(NAME gui_adapter_conformance COMMAND test_rev_adapter)
        add_test(NAME gui_platform_conformance COMMAND test_rev_platform)
        add_test(NAME gui_coordinates_1x COMMAND test_rev_coordinates 1)
        add_test(NAME gui_coordinates_2x COMMAND test_rev_coordinates 2)
        set_tests_properties(gui_platform_conformance gui_coordinates_1x gui_coordinates_2x
          PROPERTIES LABELS "gui;native_gui" TIMEOUT 90 RUN_SERIAL TRUE)
      endif()
      set_tests_properties(gui_adapter_conformance PROPERTIES LABELS "gui;native_gui" TIMEOUT 330 RUN_SERIAL TRUE)
    endif()
  endif()
  set_tests_properties(gui_layout gui_contract gui_link_boundary gui_document_layout gui_interactions gui_services gui_bindings gui_record_reconciliation gui_document_presentation gui_chrome gui_inspection gui_pattern_space
    gui_plots gui_bitmaps gui_inspection_page gui_binary_editor gui_controller gui_profile_reference gui_link_planner link_planner_curves gui_launch_command gui_launch_settings gui_application gui_overlay gui_adapter_boundary gui_boundary_regression gui_native_policy
    PROPERTIES LABELS "gui")
  if(UNIX)
    add_library(alsa_stub SHARED tests/alsa_stub.cpp)
    set_target_properties(alsa_stub PROPERTIES OUTPUT_NAME asound SOVERSION 2
      LIBRARY_OUTPUT_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/audio-test-lib")
    add_executable(test_alsa_audio tests/test_alsa_audio.cpp src/audio.cpp src/resampler.cpp)
    target_include_directories(test_alsa_audio PRIVATE include tests)
    target_link_libraries(test_alsa_audio PRIVATE alsa_stub Threads::Threads ${CMAKE_DL_LIBS})
    add_test(NAME alsa_audio_contract COMMAND test_alsa_audio)
    add_executable(test_windows_audio tests/test_windows_audio.cpp src/audio.cpp src/resampler.cpp)
    target_include_directories(test_windows_audio PRIVATE tests/winmm_stub include)
    target_compile_definitions(test_windows_audio PRIVATE _WIN32)
    if(DATAPUMP_SANITIZERS AND NOT MSVC)
      foreach(audio_test test_windows_audio test_alsa_audio alsa_stub)
        target_compile_options(${audio_test} PRIVATE -O1 -fsanitize=address,undefined -fno-omit-frame-pointer)
        target_link_options(${audio_test} PRIVATE -fsanitize=address,undefined)
      endforeach()
    endif()
    add_test(NAME windows_audio_contract COMMAND test_windows_audio)
  endif()
  find_package(Python3 QUIET COMPONENTS Interpreter)
  if(Python3_FOUND)
    if(CMAKE_OBJDUMP AND CMAKE_C_COMPILER_ID MATCHES "^(GNU|Clang|AppleClang)$"
        AND CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|amd64|AMD64|aarch64|arm64)$"
        AND CMAKE_SIZEOF_VOID_P EQUAL 8
        AND NOT DATAPUMP_SANITIZERS)
      # Inspect the optimized C witness linked into its semantic test. This
      # check is compiler/architecture-specific, not a proof about every CPU.
      add_test(NAME build_speculation_codegen COMMAND ${Python3_EXECUTABLE}
        ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_speculation_codegen.py
        --objdump ${CMAKE_OBJDUMP} $<TARGET_FILE:test_speculation>)
    endif()
    add_test(NAME build_wrapper COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_build_wrapper.py)
    add_test(NAME build_dependencies COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_build_dependencies.py)
    add_test(NAME build_release COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_release.py)
    add_test(NAME build_certify_release COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_certify_release.py)
    add_test(NAME build_windows_base COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_windows_base.py)
    if(CMAKE_HOST_SYSTEM_NAME STREQUAL "Linux")
      add_test(NAME build_release_rev_tools COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_release_rev_tools.py)
      add_test(NAME build_sdk_release COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_sdk_release.py)
      add_test(NAME build_source_sdk COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_source_sdk.py)
      add_test(NAME build_sdk_runtime COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_sdk_build.py)
      if(DATAPUMP_SDK_ROOT)
        set_tests_properties(build_source_sdk PROPERTIES ENVIRONMENT
          "DATAPUMP_TEST_CXX_COMPILER=${CMAKE_CXX_COMPILER};DATAPUMP_TEST_READELF=${CMAKE_READELF};DATAPUMP_TEST_PATCHELF=${DATAPUMP_SDK_ROOT}/bin/patchelf")
        set_tests_properties(build_sdk_runtime PROPERTIES ENVIRONMENT
          "DATAPUMP_TEST_C_COMPILER=${CMAKE_C_COMPILER};DATAPUMP_TEST_CXX_COMPILER=${CMAKE_CXX_COMPILER};DATAPUMP_TEST_OBJDUMP=${CMAKE_OBJDUMP};DATAPUMP_TEST_SYSROOT=${CMAKE_SYSROOT}")
      endif()
    endif()
    add_test(NAME fast_cable_benchmark COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_fast_cable_benchmark.py $<TARGET_FILE:pump>)
    add_test(NAME fast_cli COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_fast_cli.py $<TARGET_FILE:pump>)
    add_test(NAME fast_snr COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_fast_snr.py $<TARGET_FILE:fast_regression>)
    set_tests_properties(fast_snr PROPERTIES LABELS "fast_regression")
    add_test(NAME boundary_marker_storage COMMAND ${Python3_EXECUTABLE}
      ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_boundary_marker_storage.py
      $<TARGET_FILE:pump> ${CMAKE_CURRENT_SOURCE_DIR})
    add_test(NAME cli COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/test_cli.py $<TARGET_FILE:pump>)
  endif()
endif()
install(FILES README.md LICENSE DESTINATION share/doc/datapump)
option(DATAPUMP_INSTALL_VALIDATION_DATA "Include historical validation captures/logs in the installed bundle" OFF)
if(DATAPUMP_INSTALL_VALIDATION_DATA)
  install(DIRECTORY docs/ DESTINATION share/doc/datapump/docs)
else()
  install(DIRECTORY docs/ DESTINATION share/doc/datapump/docs PATTERN validation-data EXCLUDE)
endif()
install(FILES third_party/README.md third_party/FLTK-SOURCE.md DESTINATION share/doc/datapump/third_party)
foreach(dependency qrcodegen xz ldpc rev build-support)
  file(GLOB _dependency_notices "third_party/${dependency}/*.md"
    "third_party/${dependency}/*LICENSE*" "third_party/${dependency}/COPYING*"
    "third_party/${dependency}/UPSTREAM.json" "third_party/${dependency}/*.sha256"
    "third_party/${dependency}/debian-13-amd64.json")
  install(FILES ${_dependency_notices} DESTINATION share/doc/datapump/third_party/${dependency})
endforeach()
install(FILES third_party/fltk/COPYING DESTINATION share/doc/datapump/third_party/fltk)
install(FILES third_party/qrcodegen/LICENSE DESTINATION share/doc/datapump RENAME QR-LICENSE)
install(FILES third_party/xz/COPYING.0BSD DESTINATION share/doc/datapump RENAME XZ-LICENSE)
install(FILES third_party/xz/README.datapump.md DESTINATION share/doc/datapump RENAME XZ-SOURCE.md)
install(FILES third_party/ldpc/LICENSE DESTINATION share/doc/datapump RENAME LDPC-LICENSE)
install(FILES third_party/ldpc/WIFI-LICENSE DESTINATION share/doc/datapump RENAME LDPC-WIFI-LICENSE)
install(FILES third_party/ldpc/README.md DESTINATION share/doc/datapump RENAME LDPC-SOURCE.md)
include(cmake/BuildInfo.cmake)
include(cmake/Portable.cmake)
datapump_install_native(TARGETS ${datapump_executables})
if(BUILD_TESTING AND DATAPUMP_PORTABLE)
  add_test(NAME packaging_support COMMAND ${CMAKE_COMMAND}
    -DBINARY_DIR=${CMAKE_CURRENT_BINARY_DIR}/packaging-fixture
    -DCXX_COMPILER=${CMAKE_CXX_COMPILER}
    -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/packaging_support.cmake)
  if(NOT DATAPUMP_SANITIZERS)
    add_test(NAME native_relocation COMMAND ${CMAKE_COMMAND}
      -DBUILD_DIR=${CMAKE_CURRENT_BINARY_DIR} -DCONFIG=$<CONFIG>
      -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/package_native.cmake)
  endif()
endif()
if(BUILD_TESTING)
  if(CMAKE_HOST_SYSTEM_NAME STREQUAL "Linux")
    add_test(NAME sdk_packaging COMMAND ${CMAKE_COMMAND}
      -DBINARY_DIR=${CMAKE_CURRENT_BINARY_DIR}/sdk-packaging-fixture
      -DC_COMPILER=${CMAKE_C_COMPILER} -DOBJDUMP=${CMAKE_OBJDUMP}
      -P ${CMAKE_CURRENT_SOURCE_DIR}/tests/sdk_packaging.cmake)
    set_tests_properties(sdk_packaging PROPERTIES LABELS "build;packaging")
  endif()
  include(cmake/TestGroups.cmake)
endif()
set(CPACK_PACKAGE_NAME DataPump)
set(CPACK_PACKAGE_VERSION ${PROJECT_VERSION})
set(CPACK_PACKAGE_FILE_NAME "DataPump-${PROJECT_VERSION}-${CMAKE_SYSTEM_NAME}-${CMAKE_SYSTEM_PROCESSOR}-native")
if(DATAPUMP_BUILD_GUI)
  string(APPEND CPACK_PACKAGE_FILE_NAME "-${DATAPUMP_GUI_BACKEND}")
endif()
set(CPACK_PACKAGE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/releases")
set(CPACK_GENERATOR "TGZ;ZIP")
include(CPack)
